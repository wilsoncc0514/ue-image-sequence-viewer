# 开发规则

- 回答和开发前先核对问题前提，区分已确认事实、合理推测、个人观点和未验证信息。
- 修改代码前必须执行 `development-governance` 的开发前检查；开发中范围或风险变化时复核；交付前执行交付审查。
- 开发前说明目标、验收条件、风险等级、影响范围、最小方案、测试和回滚方式。
- 本项目按公开发布的 C 级 GUI 文件工具治理：优先保证数据安全、资源边界、并发关闭和跨平台兼容。
- 使用 Git commit、Tag 和 GitHub Release 管理版本，不再复制完整源码目录作为主要版本控制。
- 新功能优先完成可测试的最小端到端切片；配置、业务、数据、UI、工具和测试保持模块化。
- 禁止静默吞掉异常；外部输入必须限制文件大小、数量、深度、像素、队列和缓存。
- 重要写入使用同目录临时文件和原子替换；失败时不得损坏旧数据。
- 交付前分开报告规范、需求、测试、安全、已知限制、未验证项和回滚方式。
- 只读审查不授权修改、提交、推送或发布。
