[project]
name = "ue-image-sequence-viewer"
version = "3.6.0"
description = "Tkinter image-sequence QC viewer for UE rendering workflows"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "Pillow>=10.0,<13",
]

[tool.unittest]
start-directory = "图形序列查看器_v3.6/tests"

