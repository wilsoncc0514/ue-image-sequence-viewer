# 更新日志

本项目的重要版本变化记录如下，采用降序排列。

## v3.7.0 - 2026-08-10

- 调整主界面信息层级：左右侧栏采用一致的初始宽度，移除文件树无意义的横向滚动条。
- 标签文本框默认保持单行，并根据自动换行后的实际可见行数自适应高度。
- 双击图片时在系统文件管理器中打开所在目录并选中文件，覆盖 macOS、Windows 和 Linux。
- 展开序列时以可中断的短动效将目标项滚动到视区中部；靠近底部时保持自然边界，并兼容减少动态效果。
- 增加 macOS 与 Windows 双击启动器，精简冗余日志输出，仅保留可诊断的错误信息。
- 收敛 Tk/mixin 动态属性类型问题，全项目 mypy 检查达到零错误；补充 GUI、交互、启动器和生命周期测试。
- GitHub Actions 增加 Windows 验证，并统一 Ruff、mypy、单元测试和依赖安全审计。

## v3.6.1 - 2026-08-06

- 统一质检界面的中文文案、语义按钮和深色输入框，新增“暂无数据／有未导出修改／已导出”持久状态提示。
- 新增集中式 Tk 动效生命周期管理，为稳定状态提供轻量淡入和可中断的复制确认，支持菜单、快捷键和环境变量三种减少动态效果入口。
- CSV 导入改为事务式提交，解析失败时保持原质检状态不变。
- CSV 导出改为同目录临时写入和原子替换，失败时保留已有文件。
- 文件夹扫描移至单独工作线程，支持进度、取消和过期结果隔离；树节点分批构建。
- 增加 CSV、目录扫描、图片像素和渲染结果队列资源上限。
- 增加 Ruff、mypy、pip-audit、Dependabot、GUI 验收和发布治理。
- 源码目录改为稳定名称，后续通过 Git Tag 与 GitHub Release 保存版本历史。

## v3.6 - 2026-07-15

- 修正“漏光”误归入构图标签的问题，恢复光影问题 CSV 的正确导入与导出。
- 增强 CSV 空单元格兼容性，确保导入异常后恢复界面加载状态。
- 增加图片和画布尺寸校验，避免异常图片触发除零或无效缩放。
- 同步版本元数据与测试，新增 CSV、标签分类和渲染边界回归测试。
- 保留 v3.5，使用独立的 v3.6 目录发布。

## v3.5

- 采用模块化目录发布，并提供独立入口与 README。
- 完善帧序列浏览、质检、CSV 数据处理及打包运行结构。

## v3.4

- 延续模块化架构，优化发布包内容与运行稳定性。

## v3.0 - v3.3

- 将单文件工具迁移到模块化工程结构。
- 持续完善模块间功能衔接、资源组织与界面交互。

更早版本的归纳记录见 `版本迭代更新说明.txt`。
